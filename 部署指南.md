# 工程项目 AI 顾问 · 部署指南（云服务器）

> 目标：客户通过企业微信使用——需要公网云服务器 + 域名 + HTTPS。
> 本文档从零到上线完整步骤。

---

## 一、架构总览

```
[云服务器]（Linux）
  ├─ Nginx（443 HTTPS——企业微信回调入口）
  ├─ 企业微信网关（收消息/回消息/主动推送）
  ├─ advisor_engine.py（核心引擎——意图识别/图谱/多群/预警）
  ├─ advisor_llm.py（大模型增强——DeepSeek API）
  ├─ advisor.db（SQLite——客户图谱数据，按客户隔离）
  └─ cron（每日巡检/早报/预警推送）
        ↑↓ 企业微信 API
客户企业微信（项目群/单聊）
```

---

## 二、买云服务器（约 ¥60-100/月）

**推荐**：腾讯云/阿里云**轻量应用服务器**

| 项 | 配置 | 说明 |
|:---|:---|:---|
| 系统 | Ubuntu 22.04 LTS | 稳定、文档多 |
| 配置 | 2核2G（最低 2核4G 更稳） | 初期够用 |
| 带宽 | 3-5Mbps | 足够（对话消息量小） |
| 地域 | 国内（与客户近） | 企业微信要求国内合规 |
| 安全组 | 开放 80/443（22 限 IP） | 必配 |

**购买后**：改 root 密码、SSH 密钥登录、`apt update && apt upgrade`。

---

## 三、服务器环境准备

```bash
# 登录服务器
ssh root@你的服务器IP

# 基础工具
apt update && apt upgrade -y
apt install -y python3 python3-pip nginx git cron

# Python 依赖（无第三方依赖——标准库即可）
python3 --version   # 确认 3.8+

# 创建部署目录
mkdir -p /opt/ai-advisor && cd /opt/ai-advisor
```

---

## 四、部署后端代码

```bash
cd /opt/ai-advisor

# 从 GitHub 拉取
git clone https://github.com/jiawood2006/ai-project-advisor.git .
# 或手动上传 advisor_engine.py / advisor_llm.py

# 配置 DeepSeek Key（写入 .env——权限收紧）
cat > .env << 'EOF'
DEEPSEEK_API_KEY=你的key
EOF
chmod 600 .env

# 测试引擎
python3 advisor_engine.py   # 应输出多群演示
```

**验证**：`python3 -c "import advisor_engine; print('OK')"` 无报错。

---

## 五、域名 + HTTPS（企业微信回调必需）

```bash
# 1. 买域名（腾讯云/阿里云 ~¥50/年）——如 advisor.example.com
# 2. 解析：A 记录 → 服务器 IP

# 3. 申请免费证书（Let's Encrypt）
apt install -y certbot python3-certbot-nginx
certbot --nginx -d advisor.example.com

# 4. 配置 Nginx 反向代理（把 /wechat 转发给网关）
cat > /etc/nginx/sites-available/ai-advisor << 'EOF'
server {
    listen 443 ssl;
    server_name advisor.example.com;
    ssl_certificate /etc/letsencrypt/live/advisor.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/advisor.example.com/privkey.pem;

    location /wechat {
        proxy_pass http://127.0.0.1:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF
ln -s /etc/nginx/sites-available/ai-advisor /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx

# 5. 证书自动续期
echo "0 3 * * * certbot renew --quiet" | crontab -
```

---

## 六、企业微信网关（核心对接件）

### 6.1 网关职责

```
企业微信 → POST /wechat（回调：客户消息） → 网关
网关：解密 → 调 advisor_engine.handle_message(msg, group_id)
     → 调企业微信 API 回消息/主动推送
```

### 6.2 网关要点（开发件——后续实现）

| 能力 | 说明 |
|:---|:---|
| 接收消息 | 企业微信回调 URL（POST /wechat）——验证签名/解密 |
| 回复消息 | 企业微信 API（access_token + send） |
| 主动推送 | 每日巡检/预警（定时调 API 发群消息） |
| 群识别 | 消息带 ChatId → group_id → 群↔项目绑定 |
| 会话存档 | 开启企业微信会话存档（记忆图谱需要——合规版） |

### 6.3 网关配置（企业微信后台）

```
1. 客户注册企业微信（营业执照）
2. 管理后台 → 应用管理 → 自建应用「项目管家」
3. 配置：
   - 接收消息服务器：https://advisor.example.com/wechat（URL+Token+EncodingAESKey）
   - 可信域名：advisor.example.com
   - 企业可信 IP：服务器 IP
   - 成员可见范围：项目相关人员
4. 开启「会话存档」（可选——记忆图谱完整版）
```

---

## 七、定时任务（主动服务）

```bash
crontab -e
# 每日 7:30 早报
30 7 * * * cd /opt/ai-advisor && python3 -c "
import advisor_engine, json
alerts = advisor_engine.daily_check()
if alerts: 推送 alerts 到客户群（企业微信 API）
"
# 每日 21:00 日报
0 21 * * * cd /opt/ai-advisor && python3 daily_push.py
```

---

## 八、数据安全与备份

| 项 | 做法 |
|:---|:---|
| 数据库 | SQLite 单文件 /opt/ai-advisor/advisor.db |
| 备份 | 每日 cron 打包上传（oss/cos 或异地） |
| 权限 | advisor.db chmod 600——含客户数据 |
| Key | .env chmod 600——不提交 GitHub |
| 客户隔离 | group_bindings/project_id 天然隔离——校验勿跨客户 |

```bash
# 每日备份
0 2 * * * tar czf /backup/advisor_$(date +%F).tgz /opt/ai-advisor/advisor.db /opt/ai-advisor/.env
# 保留 30 天
0 3 * * 0 find /backup -name "advisor_*.tgz" -mtime +30 -delete
```

---

## 九、上线检查清单

- [ ] 服务器安全组 80/443 开放、22 限 IP
- [ ] HTTPS 证书生效（https://advisor.example.com/wechat 可访问）
- [ ] DeepSeek Key 配置（测试诊断/周报）
- [ ] 企业微信自建应用配置（回调/可信域名/IP）
- [ ] 客户扫码授权（代开发模式——应用自动装好）
- [ ] 建群 → 新建项目 → 说话记录 → 查询 → 全链路通
- [ ] cron 早报/巡检已配
- [ ] 备份 cron 已配

---

## 十、成本汇总（每月）

| 项 | 月成本 |
|:---|:---|
| 云服务器 | ¥60-100 |
| 域名（摊销） | ~¥5/月 |
| DeepSeek API（按量） | 初期 ¥10-50（客户量少） |
| **合计** | **约 ¥80-150/月** |

**一个收费客户（299/月）就覆盖成本**——2 个客户即盈利。

---

*部署完成后：客户扫码授权 → 建群 → 发「新建项目：XXX」→ 开始使用。*
