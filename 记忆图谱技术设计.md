# 工程项目 AI 顾问 · 记忆图谱技术设计（v0.1）

> 核心：每个项目一张"记忆图谱"——人说过的话、做过的事、承诺——有据可查。
> 存储：SQLite（轻量、单文件、易部署——MVP 够用；量大再换图数据库）

---

## 一、图谱结构（实体 - 关系 - 事件）

### 实体（节点）

| 实体 | 类型 | 关键属性 |
|:---|:---|:---|
| **项目** | project | name/contract/deadline/stage/progress |
| **人员** | person | name/role(公司/甲方/分包)/contact/org |
| **组织** | org | name/type(甲方/供应商/施工队)/risk_level |
| **资源** | resource | name/type(材料/设备)/spec/price/supplier |
| **任务** | task | name/owner/due/status |

### 关系（边）

| 关系 | 说明 |
|:---|:---|
| person -[in]-> project | 参与项目（角色） |
| org -[supplies]-> project | 供应商供货 |
| person -[leads]-> project | 负责项目 |
| person -[works_for]-> org | 属于某组织 |

### 事件（图谱核心——说过的话/做过的事）

| 事件类型 | 说明 | 关键属性 |
|:---|:---|:---|
| **承诺** (promise) | 谁对谁承诺了什么 | who/subject/deadline/source |
| **确认** (confirm) | 口头确认/同意 | who/subject/source |
| **决策** (decision) | 拍板/决定 | who/subject/result |
| **签证** (signoff) | 签证/签认 | who/subject/signed_by |
| **付款** (payment) | 收款/付款 | who/amount/method/status |
| **变更** (change) | 变更/增项 | who/subject/amount |
| **进度** (progress) | 进度汇报 | who/stage/percent |
| **问题** (issue) | 问题/风险 | who/subject/severity |
| **交付** (delivery) | 材料/文件交付 | who/subject/date |

**每个事件必须**：
- **who**：谁说的/做的
- **when**：时间
- **source**：原始消息（原话存档——清款依据）
- **project_id**：归属项目

---

## 二、SQLite 表设计

```sql
-- 项目
CREATE TABLE projects (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,          -- 项目名
  client TEXT,                 -- 甲方
  contract REAL DEFAULT 0,     -- 合同额
  received REAL DEFAULT 0,     -- 已收
  deadline TEXT,               -- 交付日期
  stage TEXT,                  -- 当前阶段 (R0-R8)
  manager TEXT,                -- 负责人
  status TEXT DEFAULT 'active' -- active/done/paused
);

-- 人员
CREATE TABLE persons (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  role TEXT,                   -- 公司/甲方/分包/监理
  org TEXT,                    -- 所属组织
  project_id INTEGER,          -- 关联项目
  note TEXT
);

-- 组织（甲方/供应商/施工队）
CREATE TABLE orgs (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT,                   -- client/supplier/contractor
  risk_level TEXT DEFAULT 'low', -- low/medium/high
  last_checked TEXT
);

-- 事件（记忆图谱核心）
CREATE TABLE events (
  id INTEGER PRIMARY KEY,
  project_id INTEGER NOT NULL,
  event_type TEXT NOT NULL,    -- promise/confirm/decision/signoff/payment/change/progress/issue/delivery
  who TEXT NOT NULL,           -- 谁说的/做的
  subject TEXT NOT NULL,       -- 关于什么
  amount REAL,                 -- 金额（付款/变更）
  due_date TEXT,               -- 承诺/节点日期
  status TEXT DEFAULT 'open',  -- open/done/overdue
  source TEXT,                 -- 原始消息（原话存档）
  created_at TEXT DEFAULT (datetime('now','localtime'))
);

-- 提醒/预警
CREATE TABLE alerts (
  id INTEGER PRIMARY KEY,
  project_id INTEGER,
  type TEXT,                   -- payment/stage/risk/personnel/material
  severity TEXT DEFAULT 'info',-- info/warning/critical
  content TEXT,
  status TEXT DEFAULT 'open',  -- open/resolved
  created_at TEXT DEFAULT (datetime('now','localtime'))
);

-- 术语表（客户定制——AI 学会的行业词）
CREATE TABLE glossary (
  id INTEGER PRIMARY KEY,
  term TEXT NOT NULL,          -- 术语（如"主材"）
  meaning TEXT,                -- 含义（瓷砖/地板/卫浴）
  project_id INTEGER,          -- 项目（可为空=公司级）
  created_at TEXT DEFAULT (datetime('now','localtime'))
);
```

---

## 三、对话 → 图谱管道（AI 采集）

```
用户发消息（企业微信）
  ↓
① 意图识别（分类）：
   - 报进度/回款/变更/问题（→ 记录）
   - 查询（→ 回答）
   - 预警触发（→ 核查清单）
  ↓
② 结构化提取（AI）：
   "B项目刚收了进度款20万"
   → {project: B, type: payment, who: 老板, amount: 200000, status: received}
  ↓
③ 入图谱（SQLite events 表——source 存原话）
  ↓
④ 关联分析：
   - 回款节点对比 → 逾期预警
   - 承诺到期 → 提醒
   - 人员变动 → 核查清单
  ↓
⑤ 响应/推送（企业微信）
```

**意图识别示例**（规则+AI 混合——省钱）：
- 含"收到/付了/到账/回款" → payment
- 含"答应/承诺/说好/保证" → promise
- 含"换人/离职/走了" → personnel change → 核查清单
- 含"签证/签认/确认" → signoff
- 含"问题/卡住/延误/没到" → issue
- 含"什么情况/怎么样/多少钱" → 查询
- 其他 → 记录或问

---

## 四、关键查询（图谱价值兑现）

```sql
-- 清款依据：甲方承诺过什么
SELECT who, subject, due_date, source, created_at
FROM events
WHERE project_id=? AND event_type='promise'
  AND status='open'
ORDER BY created_at;

-- 逾期应收
SELECT p.name, e.subject, e.amount, e.due_date
FROM events e JOIN projects p ON e.project_id=p.id
WHERE e.event_type='payment' AND e.status='open'
  AND e.due_date < date('now');

-- 人员变动核查
SELECT * FROM events
WHERE event_type='change' AND subject LIKE '%人员%'
  OR subject LIKE '%换人%' OR subject LIKE '%离职%';

-- 风险预警（组织）
SELECT * FROM orgs WHERE risk_level IN ('medium','high');
```

---

## 五、扩展（后期）

- 图数据库（Neo4j）——图谱大时做多跳查询
- 风险雷达自动采集（企查查/执行网/新闻 → orgs.risk_level）
- 多群隔离（project_id 天然隔离——群↔项目映射）
- 免费/收费（events 数量/群数按档位限制）
